# MIRANCE — Dependencies
# Install: pip install -r requirements.txt

# USB Communication
pyusb>=1.2.0
libusb-package>=1.0.26.0       # Bundles libusb for Windows — no manual DLL needed

# iPhone Detection & Communication
pymobiledevice3>=2.0.0

# Video Decoding (H.264 via FFmpeg)
av>=12.0.0

# GUI Framework
PyQt6>=6.6.0

# OpenGL Rendering
PyOpenGL>=3.1.7

# Audio Playback
sounddevice>=0.4.6

# Numerical (frame buffers)
numpy>=1.24.0

# Image processing (screenshot fallback)
Pillow>=10.0.0
