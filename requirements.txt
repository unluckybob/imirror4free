# IMIRROR4FREE Dependencies
# ========================

# Apple USB protocol - device detection, pairing, DVT services
pymobiledevice3>=4.0.0

# Raw USB access for Valeria protocol (Phase 2)
pyusb>=1.2.1

# Application framework
PyQt6>=6.6.0

# OpenGL rendering
PyOpenGL>=3.1.7
PyOpenGL-accelerate>=3.1.7

# Video decode (FFmpeg with hardware acceleration)
av>=12.0.0

# Fast frame buffer operations
numpy>=1.26.0

# Image processing (screenshot capture)
Pillow>=10.0.0

# Audio playback
sounddevice>=0.4.6

# Windows-specific USB driver helper
# libusb is bundled with pyusb on Windows via libusb-package
libusb-package>=1.0.26.1; sys_platform == "win32"

# Build tools (optional - for creating .exe)
# pyinstaller>=6.0.0
